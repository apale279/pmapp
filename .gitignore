# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets / env (never commit real keys)
.env
.env.*
!.env.example

# Internal prompts & office binaries (optional local notes; keep out of git)
Prompt/

# Office / Excel lock & temp (never commit)
~$*
**/~$*

# Local archives (not for repo)
public.zip

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: restringere in produzione (RBAC per rank / manifestazione).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
